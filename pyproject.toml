[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aitrace"
version = "0.1.0"
description = "Trace event aggregation for AI pipelines"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/aitrace"]
